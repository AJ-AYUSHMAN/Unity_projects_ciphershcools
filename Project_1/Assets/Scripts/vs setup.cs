using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class vssetup : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       Debug.Log("hello this is Ayushman");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
